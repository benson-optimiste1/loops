const videos = ['cats', 'news', 'games', 'sports']
// console log each of these videos to the screen
// Make sure it works for any lenght of array
for(let i = 0; i < videos.length; i++){
    console.log(videos[i])
}
